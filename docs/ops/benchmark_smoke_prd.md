# AutoDev Smoke PRD

## Goals
- Verify a minimal generation path is healthy after an LLM provider swap.
- Keep the run short enough for iterative local benchmarking.

## Features
- Create a helper function `add(a: int, b: int) -> int`.
- Add one unit test for integer addition.

## Acceptance Criteria
- Generate a runnable Python CLI project.
- Include at least one passing unit test.
- Exit with success and write `.autodev/REPORT.md`.
